# Override the API base URL when running the frontend against a non-default
# backend (e.g. a deployed environment, a remote dev box, or a docker-compose
# port other than 5154). Default is http://localhost:5154.
VITE_API_BASE_URL=http://localhost:5154
